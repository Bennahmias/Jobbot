name: Run Telegram Bot

on:
  schedule:
    - cron: "*/5 * * * *"  # Runs every 5 minutes
  workflow_dispatch:  # Allows manual execution

jobs:
  run-bot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'  

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Ensure message_tracker.json exists  # ✅ New step
        run: |
          if [ ! -f message_tracker.json ]; then
            echo '{}' > message_tracker.json  # Creates an empty JSON file if missing
          fi

      - name: Run bot
        env:
          TELEGRAM_API_ID: ${{ secrets.API_ID }}
          TELEGRAM_API_HASH: ${{ secrets.API_HASH }}
          SESSION_STRING: ${{ secrets.SESSION_STRING }}
        run: python bot.py

      - name: Upload updated message history
        uses: actions/upload-artifact@v2
        with:
          name: message_tracker
          path: message_tracker.json
